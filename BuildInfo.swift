// Substituído pelo workflow a cada build (passo "Carimbar a versao no app").
// Este conteúdo só aparece num build feito fora do GitHub Actions.
enum BuildInfo {
    static let commit = "local"
    static let date   = "sem data"
    static let run    = "0"
}
